FROM node:18-alpine

WORKDIR /usr/src/app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy app sources (will be overridden by volume mount in dev)
COPY . .

EXPOSE 5001

CMD ["npm", "run", "dev"]
